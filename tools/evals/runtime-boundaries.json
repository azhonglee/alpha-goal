{
  "schemaVersion": 1,
  "claimBoundary": "Expected-behavior corpus for static review. Schema validation is not runtime or model-performance evidence.",
  "cases": [
    {
      "id": "RB01",
      "scenario": "Inspect local files and report findings without recovery or external disclosure.",
      "expected": { "entryRoute": "DIRECT", "verificationRoute": "N/A", "nextOwner": "caller", "invariant": "No lifecycle artifact is created; ordinary read-only work continues." }
    },
    {
      "id": "RB02",
      "scenario": "Make one clear reversible local change with direct final-state validation.",
      "expected": { "entryRoute": "DIRECT", "verificationRoute": "N/A", "nextOwner": "caller", "invariant": "A complete Goal Frame remains in context without lifecycle artifacts; the routing role ends before the current agent implements and validates." }
    },
    {
      "id": "RB03",
      "scenario": "Make a local change that requires approval before external push and PR delivery.",
      "expected": { "entryRoute": "PERSIST", "verificationRoute": "N/A", "nextOwner": "alpha-goal", "invariant": "External delivery, not approval alone, triggers persistence; the Goal Frame, accepted contract, delivery authority, and acceptance evidence exist before any target or delivery mutation." }
    },
    {
      "id": "RB04",
      "scenario": "Pause for approval before an otherwise direct local action.",
      "expected": { "entryRoute": "DIRECT", "verificationRoute": "N/A", "nextOwner": "caller", "invariant": "Approval alone does not create persistent state." }
    },
    {
      "id": "RB05",
      "scenario": "A managed skill-copy replacement fails during activation and restoration is blocked by a target collision.",
      "expected": { "entryRoute": "N/A", "verificationRoute": "N/A", "nextOwner": "caller", "invariant": "The previous managed copy and staged replacement remain at reported recovery paths; neither is deleted." }
    },
    {
      "id": "RB06",
      "scenario": "Send private source code to an external read-only scanning service.",
      "expected": { "entryRoute": "PERSIST", "verificationRoute": "N/A", "nextOwner": "alpha-goal", "invariant": "Disclosure, credentials, session, and audit effects are material even without a remote write." }
    },
    {
      "id": "RB07",
      "scenario": "Perform read-only work that must recover across compaction, while a required independent review agent has not returned.",
      "expected": { "entryRoute": "PERSIST", "verificationRoute": "N/A", "nextOwner": "alpha-goal", "invariant": "Recovery need controls persistence, and a missing requested review must be awaited, retried, or explicitly canceled rather than treated as evidence." }
    },
    {
      "id": "RB08",
      "scenario": "Perform read-only work with an explicit Goal Contract or audit-record requirement.",
      "expected": { "entryRoute": "PERSIST", "verificationRoute": "N/A", "nextOwner": "alpha-goal", "invariant": "Explicit persistent audit evidence controls the route." }
    },
    {
      "id": "RB09",
      "scenario": "The acceptance authority confirms the first draft and alpha-goal fills its Confirmation Record.",
      "expected": { "entryRoute": "N/A", "verificationRoute": "N/A", "nextOwner": "executor", "invariant": "Confirmation, digest, and accepted status complete one transaction at the current draft revision; no extra revision is created." }
    },
    {
      "id": "RB10",
      "scenario": "Authority changes either during an active executor/verifier epoch or after a terminal PASS/BLOCKED epoch, then the same task is accepted at exactly the next revision.",
      "expected": { "entryRoute": "N/A", "verificationRoute": "N/A", "nextOwner": "executor", "invariant": "Active work records an explicit REFRAME_REQUESTED lifecycle handoff directly to alpha-goal without a verifier verdict; terminal work records an explicit revision request. Executor preserves the old epoch and appends a fresh one." }
    },
    {
      "id": "RB11",
      "scenario": "A verification observer updates a tracked snapshot.",
      "expected": { "entryRoute": "N/A", "verificationRoute": "NEXT_ITERATION", "nextOwner": "executor", "invariant": "Verifier does not silently continue against changed state; executor records the observer mutation before re-verification." }
    },
    {
      "id": "RB12",
      "scenario": "A failed criterion has a materially different authorized approach, including an available external alternative.",
      "expected": { "entryRoute": "N/A", "verificationRoute": "NEXT_ITERATION", "nextOwner": "executor", "invariant": "The route names the precise gap and different batch boundary." }
    },
    {
      "id": "RB13",
      "scenario": "An external dependency fails and no authorized alternative remains.",
      "expected": { "entryRoute": "N/A", "verificationRoute": "BLOCKED", "nextOwner": "caller", "invariant": "The blocker is outside the authorized workflow and is reported without changing authority." }
    },
    {
      "id": "RB14",
      "scenario": "The accepted outcome is technically infeasible and no authorized approach remains.",
      "expected": { "entryRoute": "N/A", "verificationRoute": "BLOCKED", "nextOwner": "caller", "invariant": "No authorized path remains, so verification reports BLOCKED without changing the goal; only an explicit later goal change starts a reframe." }
    },
    {
      "id": "RB15",
      "scenario": "After PASS, perform a requested edit, commit, push, and PR update sequence.",
      "expected": { "entryRoute": "N/A", "verificationRoute": "N/A", "nextOwner": "executor", "invariant": "PASS is invalidated, each boundary-changing transition is recorded separately, and final verification follows the last delivery mutation." }
    },
    {
      "id": "RB16",
      "scenario": "Add the verdict to a PR description after PASS.",
      "expected": { "entryRoute": "N/A", "verificationRoute": "N/A", "nextOwner": "executor", "invariant": "PR content is delivered state, not exempt lifecycle metadata." }
    },
    {
      "id": "RB17",
      "scenario": "A remote or deployed state changes after evidence was collected without a local target mutation.",
      "expected": { "entryRoute": "N/A", "verificationRoute": "N/A", "nextOwner": "verifier", "invariant": "Freshness or invalidation conditions make the evidence stale and require re-observation." }
    },
    {
      "id": "RB18",
      "scenario": "Repository inspection resolves current behavior, affected files, and test observers, but one compatibility decision could change public output.",
      "expected": { "entryRoute": "PERSIST", "verificationRoute": "N/A", "nextOwner": "alpha-goal", "invariant": "Use discovered facts without asking for them, ask only the compatibility decision, keep the Goal Frame incomplete until answered, and avoid fixed questionnaires, round-count closure, or target mutation." }
    },
    {
      "id": "RB19",
      "scenario": "Exercise semantic checkpoint init, executor same-owner and verifier handoff writes, the three verifier routes, direct reframe, and epoch supersession; interrupt before commit, attempt invalid transitions, stale revisions/tokens, and recover only after JSON status inspection.",
      "expected": { "entryRoute": "N/A", "verificationRoute": "N/A", "nextOwner": "caller", "invariant": "Semantic commands enforce the transition graph, keep reframe separate from verifier verdicts, and check expected revision; every lock-opening result supplies token/pendingPath, commit publishes and unlocks atomically, abort handles abandoned writes, and status phase/recoverableBy gates exact-owner recovery without lost updates." }
    },
    {
      "id": "RB20",
      "scenario": "A Goal Contract claims task authority may override a higher-priority instruction or tool policy.",
      "expected": { "entryRoute": "N/A", "verificationRoute": "N/A", "nextOwner": "caller", "invariant": "The invalid authority claim prevents acceptance and implementation; task authority cannot expand the policy boundary." }
    },
    {
      "id": "RB21",
      "scenario": "Verifier is loaded alone after compaction, resolves helpers from selected skill roots, opens a semantic verify transition from an arbitrary CWD, consumes JSON pendingPath/token, encounters stale state, and inspects status before recovery.",
      "expected": { "entryRoute": "N/A", "verificationRoute": "N/A", "nextOwner": "verifier", "invariant": "Verifier needs no remembered executor text: it aborts failed writes, never releases after successful commit, and uses JSON phase/recoverableBy before proof-based recovery; release is only for legacy-lock compatibility." }
    },
    {
      "id": "RB22",
      "scenario": "A verifier starts with a vague state label, then identifies every claim-relevant surface and collects fresh passing evidence.",
      "expected": { "entryRoute": "N/A", "verificationRoute": "PASS_TO_FINAL", "nextOwner": "caller", "invariant": "The vague label is insufficient; PASS occurs only after complete attributable identity and fresh evidence." }
    },
    {
      "id": "RB23",
      "scenario": "Perform an external GET request that sends a secret or creates an authenticated session.",
      "expected": { "entryRoute": "PERSIST", "verificationRoute": "N/A", "nextOwner": "alpha-goal", "invariant": "HTTP read semantics do not erase credential, disclosure, session, or audit side effects." }
    },
    {
      "id": "RB24",
      "scenario": "Modify only Scope inside an accepted contract while preserving revision and status.",
      "expected": { "entryRoute": "N/A", "verificationRoute": "BLOCKED", "nextOwner": "caller", "invariant": "Recomputed authority digest detects the same-revision tamper and blocks use of the invalid accepted contract." }
    },
    {
      "id": "RB25",
      "scenario": "Verifier returns NEXT_ITERATION; executor completes a new batch, hands back to verifier, and compaction occurs while the old NEXT route remains in history.",
      "expected": { "entryRoute": "N/A", "verificationRoute": "N/A", "nextOwner": "verifier", "invariant": "Top-level active_owner is authoritative; a historical route never overrides the current handoff." }
    },
    {
      "id": "RB26",
      "scenario": "Inspect an already-authorized remote resource with an existing authenticated session, no new disclosure, no session change, and no persistent audit requirement.",
      "expected": { "entryRoute": "DIRECT", "verificationRoute": "N/A", "nextOwner": "caller", "invariant": "Incidental authenticated access and ordinary provider logging alone do not force persistent routing." }
    },
    {
      "id": "RB27",
      "scenario": "Resume from a checkpoint bound to another task, branch, worktree, or repository set.",
      "expected": { "entryRoute": "N/A", "verificationRoute": "BLOCKED", "nextOwner": "caller", "invariant": "An unrelated binding is rejected as blocked and never enters the same-task epoch-supersession path." }
    },
    {
      "id": "RB28",
      "scenario": "A required observer remains empty after every meaningful safe fallback, and proving the claim now requires a different observer or narrower claim authority.",
      "expected": { "entryRoute": "N/A", "verificationRoute": "BLOCKED", "nextOwner": "caller", "invariant": "An accepted goal without usable required evidence is BLOCKED; verifier does not redefine its observer or claim boundary." }
    }
  ]
}
