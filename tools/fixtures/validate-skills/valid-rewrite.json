{
  "shouldPass": true,
  "inheritRoot": true,
  "replacements": [
    {
      "file": "skills/alpha-goal/SKILL.md",
      "search": "Implementation starts only after a user-accepted Goal Contract hands off to `executor` skill.",
      "replace": "Execution starts only after a user-accepted Goal Contract hands off to `executor` skill."
    }
  ]
}
