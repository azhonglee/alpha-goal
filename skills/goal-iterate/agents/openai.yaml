interface:
  display_name: "Goal Iterate"
  short_description: "Plan execute feedback loop"
  default_prompt: "Use $goal-iterate to run one dynamic planning, execution, and feedback loop under the current Goal Contract."

policy:
  allow_implicit_invocation: false
