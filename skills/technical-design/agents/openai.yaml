interface:
  display_name: "Technical Design"
  short_description: "Design first, then hand structured context to Alpha Goal"
  default_prompt: "Use $technical-design to create and review technical_design.md, then return a DESIGN_READY packet for the caller to start Alpha Goal."
