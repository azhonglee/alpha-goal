# Ledger

Use `.alpha-goal/control-state/latest.md` as the stable latest route entry when work crosses skills/turns, mutates, uses subagents, has material risk, contains indicators/disturbances/adaptive learning/hierarchy, or supports final/ready/safe/complete/repair claims. Chat-only is allowed only for one-turn low-risk read-only work or when writes are forbidden/unavailable; state that limit.

Latest Control Route:
- Reference:
- Current state:
- Last error signal:
- Control law:
- Sensor feedback:
- Route decision:
- Next state:
- Artifact registry:
- Adaptive learning:
- Selected skill:
- Boundary:
- Disturbance:
- User-owned decisions:
- Blocked downstream action:
- Claim boundary:
- Next action:

Artifact registry may point to `.alpha-goal/context`, `.alpha-goal/models`, `.alpha-goal/synthesis`, `.alpha-goal/iterations`, `.alpha-goal/evidence`, and `.alpha-goal/verification`. Put Discovery Interview notes there when facts, conflicts, confirmations, or user-owned decisions must survive handoff.
