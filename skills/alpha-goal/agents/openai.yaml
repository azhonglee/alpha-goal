interface:
  display_name: "Alpha Goal"
  short_description: "Frame and route goal work"
  default_prompt: "Use $alpha-goal to clarify the Goal Contract, and route iterate work."

policy:
  allow_implicit_invocation: true
