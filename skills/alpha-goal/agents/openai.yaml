interface:
  display_name: "Alpha Goal"
  short_description: "形成可执行目标契约"
  default_prompt: "Use $alpha-goal to form a bounded goal contract with reference state, scope, non-goals, evidence, and handoff boundary."
