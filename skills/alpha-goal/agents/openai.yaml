interface:
  display_name: "Alpha Goal"
  short_description: "Route the closed-loop skill suite"
  default_prompt: "Use $alpha-goal to choose the next skill route, stability boundary, indicator, disturbance, hierarchy, and learning gates."
