interface:
  display_name: "Verify"
  short_description: "Judge evidence and claim boundary"
  default_prompt: "Use $verify to compare fresh evidence against the Goal Contract and decide the final claim boundary."
