interface:
  display_name: "Goal Loop"
  short_description: "Route goal loops by intent"
  default_prompt: "Use $goal-loop to identify the loop type, choose frame/iterate/verify, and keep evidence-bound claims honest."

policy:
  allow_implicit_invocation: true
