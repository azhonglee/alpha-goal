interface:
  display_name: "Goal Loop"
  short_description: "Frame and route goal work"
  default_prompt: "Use $goal-loop to classify the goal type, frame the Goal Contract, and route iterate/verify work."

policy:
  allow_implicit_invocation: true
