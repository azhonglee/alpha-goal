interface:
  display_name: "Goal Loop"
  short_description: "Route frame, iterate, verify stages"
  default_prompt: "Use $goal-loop to route this coding task through goal-frame, goal-iterate, and goal-verify."
