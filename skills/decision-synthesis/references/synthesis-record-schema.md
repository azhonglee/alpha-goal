# 决策综合记录结构定义

字段：复杂度类型、核心张力、综合研判工作台、综合轮次、指标转译、用户自有决策、推荐方向、风险、所需证据、台账更新、路由。`persisted` / `audited` 写 `.alpha-goal/YYYYMMDD-<slug>/decision-synthesis.md`。需要执行时先到 `goal-contract`；不能把下一路由写成 `control-loop`。
