interface:
  display_name: "Goal Review"
  short_description: "Optional independent challenge"
  default_prompt: "Use $goal-review to independently challenge alpha-goal feedback, scope, architecture, diff, or readiness risk."

policy:
  allow_implicit_invocation: false
