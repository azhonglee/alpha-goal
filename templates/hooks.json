{
  "hooks": {
    "PostCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": ": 'codex-alpha-goal-compact-recovery:v4'; printf '%s\\n' 'Alpha Goal recovery:' 'Reload applicable skills; remembered text is stale. Use only an explicit current artifact path; never infer by recency.' 'checkpoint.jsonl: read the last valid JSON record and follow active_owner. Send only the current result as plain text through bash <executor-root>/scripts/checkpoint-lock.sh; the helper constructs JSON. After any return or interruption, read the last valid record again. checkpoint.md is legacy and stays on the compatibility path. Conflict stops. caller reports PASS/BLOCKED/GOAL_CHANGED as terminal. Later work uses a new Alpha Goal task directory.' 'goal-contract.md without checkpoint.jsonl: draft loads alpha-goal; accepted with valid completeness/digest loads alpha-goal to confirm the goal is unchanged, then executor initializes; otherwise stop. No explicit path means normal routing.'",
            "statusMessage": "Refreshing Alpha Goal recovery context"
          }
        ]
      }
    ]
  }
}
