{
  "hooks": {
    "PostCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": ": 'codex-alpha-goal-compact-recovery:v1'; printf '%s\\n' 'Compact recovery policy:' 'After compaction, decide whether $alpha-goal, $executor, or $verifier applies before acting; load the matching skill and treat remembered skill text as stale.' 'For active Alpha Goal work, read goal-contract.md first; read technical_design.md when present for implementation, repair, refactor, hardening, cross-file behavior, interface/data-model changes, or material risk after Goal Contract Confirmation Gate selects technical design; use an explicit task artifact path when available; otherwise scan task directories and stop on multiple candidates instead of selecting the newest task; executor must persist its acceptance checklist and every important slice's raw evidence in checkpoint.md before verifier handoff.' '$executor requires an accepted Goal Contract. $verifier runs after every important slice, owns evidence classification and route semantics, and the calling Agent manages native Goal lifecycle updates from terminal routes.' 'If none applies, continue normally.'",
            "statusMessage": "Refreshing Alpha Goal compact recovery policy"
          }
        ]
      }
    ]
  }
}
