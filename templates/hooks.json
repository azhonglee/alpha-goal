{
  "hooks": {
    "PostCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": ": 'codex-alpha-goal-compact-recovery:v3'; printf '%s\\n' 'Alpha Goal compact recovery:' 'Treat remembered skill text as stale; use only an explicit current artifact path.' 'For checkpoint.md, inspect the canonical checkpoint.md.lock before writing; pending records do not block. If it survives, load executor checkpoint-lock instructions for proof-based pre/post-snapshot recovery before dispatching the current owner; never reclaim by age alone. Validate the bound contract and current epoch.' 'Resume only from top-level active_owner; the current epoch completed route is a consistency check and never overrides owner. active_owner=verifier loads verifier; executor loads executor; alpha-goal loads alpha-goal; caller reports BLOCKED or rechecks PASS_TO_FINAL identity and freshness. A later target or delivery mutation resumes executor. Route/owner conflict or invalid binding reports BLOCKED to caller.' 'Exception: load executor for guarded exact-next-revision epoch supersession after either REFRAME_REQUESTED/alpha-goal or terminal PASS_TO_FINAL|BLOCKED/caller with an explicit terminal revision request. Ignore routes from prior epochs.' 'For goal-contract.md alone: draft loads alpha-goal; accepted with a valid authority digest loads executor to initialize checkpoint.md; invalid accepted binding reports BLOCKED to caller. Never infer the active task from directory recency. Without an explicit artifact path, continue normal routing.'",
            "statusMessage": "Refreshing Alpha Goal recovery context"
          }
        ]
      }
    ]
  }
}
