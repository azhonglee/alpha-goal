{
  "hooks": {
    "PostCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": ": 'codex-alpha-goal-compact-recovery:v3'; printf '%s\\n' 'Alpha Goal recovery:' 'Reload applicable skills; remembered text is stale. Use only an explicit current artifact path; never infer by recency.' 'checkpoint.md: load executor lock instructions, inspect status/binding, and never reclaim by age. Follow top-level active_owner; current route only validates owner, historical routes are ignored, and conflict stops. caller reports BLOCKED or rechecks PASS identity/freshness; a later mutation resumes executor. Supersede only from explicit current context.' 'goal-contract.md without checkpoint: draft loads alpha-goal; accepted with valid completeness/digest loads executor to initialize; otherwise stop. No explicit path means normal routing.'",
            "statusMessage": "Refreshing Alpha Goal recovery context"
          }
        ]
      }
    ]
  }
}
