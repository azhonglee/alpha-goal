{
  "hooks": {
    "PostCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": ": 'codex-alpha-goal-compact-recovery:v4'; printf '%s\\n' 'Alpha Goal recovery:' 'Reload applicable skills; remembered text is stale. Use only an explicit current artifact path; never infer by recency.' 'checkpoint.md: inspect recorded identity and follow top-level active_owner. Re-read before any sequential write; stale revision, owner, or changed content stops the write. If another writer may be active, do not write. Legacy alpha-goal owner loads executor only to terminate it to caller. caller reports PASS/BLOCKED/GOAL_CHANGED as terminal. Later work uses a new Alpha Goal task directory.' 'goal-contract.md without checkpoint: draft loads alpha-goal; accepted with valid completeness/digest loads alpha-goal to confirm the goal is unchanged, then executor initializes; otherwise stop. No explicit path means normal routing.'",
            "statusMessage": "Refreshing Alpha Goal recovery context"
          }
        ]
      }
    ]
  }
}
