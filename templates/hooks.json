{
  "hooks": {
    "PostCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": ": 'codex-alpha-goal-compact-recovery:v4'; printf '%s\\n' 'Alpha Goal stage recovery:' 'Use only the exact task directory preserved in current context; never scan or guess another task directory.' 'If checkpoint.md exists, read goal-contract.md and checkpoint.md. active_owner executor loads $executor; active_owner verifier loads $verifier; legacy active_owner alpha-goal loads $executor for termination. caller or a terminal checkpoint needs no lifecycle skill.' 'If goal-contract.md exists without checkpoint.md, read goal-contract.md. status draft loads $alpha-goal; status accepted loads $executor to initialize execution.' 'If neither artifact exists, use $alpha-goal only when goal framing or persistent routing is needed; otherwise continue normally.' 'After selecting the stage, load that skill and follow its current instructions; remembered skill text is stale.'",
            "statusMessage": "Recovering Alpha Goal stage"
          }
        ]
      }
    ]
  }
}
